on: [push]

jobs:
  publish_docs:
    name: Publish docs
    runs-on: ubuntu-latest
    steps:
      - name: Publish docs
        uses: docploy/docploy-action@latest
        with:
          baseUrl: 'https://cvuong.github.io/test-action/'
          docsDir: 'docs'
