on: [push]

jobs:
  publish_docs:
    name: Publish docs
    runs-on: ubuntu-latest
    steps:
      - name: Publish docs
        uses: docploy/docploy-action@v1
        with:
          baseUrl: 'https://cvuong.github.io/test-action/'
          docsDir: 'docs'
